services:
  minecraft-server:
    build: .
    container_name: minecraft-server
    # restart: unless-stopped
    # volumes:
    #   - ./server-files:/minecraft/server
    ports:
      - "25565:25565"
    tty: true
