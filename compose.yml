services:
  minecraft-server:
    build: .
    container_name: minecraft-server
    restart: unless-stopped
    volumes:
      - minecraft-data:/minecraft/server
    ports:
      - "25565:25565"
    tty: true

volumes:
  minecraft-data: